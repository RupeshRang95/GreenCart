/**
 * GreenCart receipt scope: Canadian grocery trips.
 * Products are modeled as sold in Canada — domestic (lower distance) vs imported / international (supply chain).
 */

export const USER_HOME_COUNTRY_KEY = "canada" as const;

export const USER_HOME_COUNTRY_LABEL = "Canada";
